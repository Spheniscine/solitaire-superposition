[package]
name = "math-macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
katex = { version = "0.4", default-features = false, features = ["duktape"] }
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }